УДАЛИТЬ результат этого запроса: "На телефоне, когда открывается клавиатура, над клавиатурой появляется окно с стрелочками вверх и вниз, и галочка срава, но если свернуть клавиатуру или начать писать в другом поле, то появляется уже стандартное отображение с другими иконками, ключик, банковская карта и местоположение , а так же справа кнопка сворачивания клавиатуры , это всё в гугл браузере на айфоне. Так вот убери первый сценарий отовсюду, пусть отображается стандартный визуал как в втором случае"

УДАЛИТЬ результат этого запроса: "Проблема почти ушла, но всё равно, при открытии клавиатуры, сначала на пол секунды появляется старый сценарий со стрелочками, а потом уже появляется то что нужно"

УДАЛИТЬ результат этого запроса: "Проблема не ушла, всё равно при открытии клавиатуры появляется сначала стрелочки и галочка сверху, а потом перключается на ключик, карту, геолокацию и внопку сворачивание клавиатуры"

УДАЛИТЬ результат этого запроса: "Вообще эта проблема появилась из-за того что раньше пытались устранить фон под этими кнопками, раньше фон был в виде просто чёрной области в тёмном режиме и белой области в белом режиме и пытались как-то устранить, устранить получилось но вместе с этим вот применили вот эту логику появления навигационных этих стрелок и галочки, что собственно как бы ну ломает стандартное поведение системы, возможно тебе это чем-то поможет, хотелось бы чтобы работало как обычно в других приложениях, чтобы логика полностью сохранялась стандартная"

УДАЛИТЬ результат этого запроса: "К сожалению не помогло, всё равно проблема осталась"

УДАЛИТЬ результат этого запроса: "Баг с дерганием клавиатуры происходит в Google Chrome на iOS (так как на iPhone все браузеры используют системный WebKit и клавиатуру iOS): При клике на инпут ввода события/заметки сначала появляется панель со стрелочками ▲ ▼, а через ~0.5 сек она сбрасывается на стандартную.

Это происходит из-за того, что в момент фокуса JS делает re-render компонента или изменяет DOM, и iOS пересчитывает форму на лету.

Исправь это следующим образом:

В модалках и окнах создания/редактирования событий календаря:

Если открывается модалка с инпутом, сначала полностью отрендери модалку и инпут в DOM, и только ПОСЛЕ завершения рендера ставь фокус на инпут (а не одновременно с открытием). Убери из обработчиков onFocus и onClick любые вызовы setState/update, которые вызывают повторный рендеринг этого инпута или его родительского контейнера. Для самого инпута:

Убедись, что инпут НЕ обернут в тег

(или добавь тегу атрибуты onsubmit="return false;" и novalidate). Добавь на инпут атрибуты: autocomplete="off" и tabindex="-1" (если рядом нет других инпутов, между которыми нужно переключаться стрелочками). Если используешь автофокус (autofocus), убери его и используй контролируемый фокус через ref.current.focus() строго после того, как компонент полностью смонтирован."
УДАЛИТЬ результат этого запроса: "Проблема с переключением панели клавиатуры на iOS осталась. Так как JS-логика вычищена, проблема на 100% вызвана спецификой CSS/WebKit на iOS.

Сделай следующие проверки и исправления в CSS и верстке:

FONT-SIZE (Критично для iOS):

Убедись, что абсолютно у всех инпутов, textarea и полей ввода на мобильных устройствах font-size составляет минимум 16px (16px или 1rem). Если шрифты были 12px-15px, iOS вызывает автозум вьюпорта, что ломает панель клавиатуры. USER-SELECT:

Проверь, нет ли на body, контейнерах модалок или сетке календаря свойства -webkit-user-select: none;. Если оно есть, убедись, что для полей ввода явно задано -webkit-user-select: auto; или -webkit-user-select: text;, либо временно отключи user-select на родителях для проверки. CSS TRANSITIONS / ANIMATIONS:

Отключи любые CSS transitions (transition: all ...) у модалок и инпутов в момент фокуса (:focus) или открытия. Позиция и размеры инпута не должны плавно меняться во время появления клавиатуры. ДИНАМИЧЕСКИЙ DOM:

Проверь: инпут существует в DOM ЗАРАНЕЕ, или он создается/заменяет собой текст именно в момент клика по ячейке/событию? Если инпут создается по клику, переделай так, чтобы инпут уже рендерился в DOM (пусть даже скрытым через opacity/visibility, но не display:none)."

УДАЛИТЬ результат этого запроса: "Нам нужно вернуть 100% СТАНДАРТНОЕ нативное поведение клавиатуры iOS (Safari / Chrome), БЕЗ каких-либо костылей и искусственных ограничений.

Сделай следующее:

УДАЛИ ВСЕ КОСТЫЛИ И ИСКУССТВЕННЫЕ ХАКИ:

Убери все индивидуальные теги , которые оборачивали каждый инпут по отдельности. Убери все искусственные tabIndex={-1}, если они навешивались ради отключения стрелок. Удали из обработчиков onFocus, onClick и onTouchStart абсолютно любую JS-логику, которая меняет атрибуты поля (readOnly, disabled, type), состояние (state) или структуру DOM. ВЕРНИ ЕСТЕСТВЕННУЮ СТРУКТУРУ HTML:

Поля ввода должны быть обычными стандартными  или . Не создавай инпуты «на лету» при клике — они должны уже статично существовать в DOM к моменту тапа. ПОЧЕМУ ЭТО ПОЧИНИТ ПРОБЛЕМУ:

Когда DOM полностью статичен и JS не меняет параметры инпута во время фокуса, iOS сразу (в первую же миллисекунду) определяет контекст и открывает именно тот вариант клавиатуры, который положен по стандарту (со стрелочками или без), без каких-либо задержек в 0.5 секунды и перерисовок."

УДАЛИТЬ результат этого запроса: "Точная причина дергания панели клавиатуры на iOS (Safari / Chrome iOS) установлена: При фокусе на инпут системный модуль iOS AutoFill (менеджер паролей, карт и адресных данных Apple) асинхронно ищет данные для текущего домена в течение ~0.5 секунды. В первую миллисекунду WebKit рисует базовые стрелочки, а через полсекунды — подменяет их на панель автозаполнения (с иконками Ключа, Карты и Локации).

Чтобы полностью отключить асинхронный поиск iOS AutoFill и сразу показывать стандартную клавиатуру без задержек, сделай следующее:

АТРИБУТЫ ДЛЯ ОТКЛЮЧЕНИЯ AUTOFILL: На все инпуты и textarea (целей, заметок, событий) жестко установи следующие атрибуты:

autocomplete="off" autocorrect="off" spellcheck="false" data-1p-ignore="true" ОЧИСТКА ИМЕН И ИДЕНТИФИКАТОРОВ (КРИТИЧНО ДЛЯ iOS):

Проверь атрибуты name, id и placeholder у всех полей ввода. Убедись, что в них НЕТ подстрок: "user", "name", "email", "pass", "card", "login", "address", "phone", "title". Если iOS видит подобные слова в атрибутах name/id, она принудительно запускает модуль AutoFill и производит фоновый поиск. Задай инпутам абстрактные имена, например: name="todo-item-input" или name="calendar-note-entry"."

ВСË ОСТАЛЬНОЕ ОСТАВИТЬ!